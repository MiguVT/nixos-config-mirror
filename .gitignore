# Nix build artifacts (result is a symlink to the last build)
result
result-*

# Local, machine-specific overrides (keep out of VCS)
local/
*.local.nix

# Secrets (agenix / sops-nags / nix-secrets)
secrets/*
# Allow only the currently tracked SOPS bundle; add future encrypted files explicitly.
!secrets/secrets.yaml

# Editor and OS cruft
*~
.#*
.*.swp
.DS_Store

# Local scratch / reference material (e.g. cloned docs)
.temp/
